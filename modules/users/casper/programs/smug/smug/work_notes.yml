session: work_notes

root: ~/git/technative_b.v./technative-linny
startup_pane: 1

stop:

windows:
  - name: sync
    layout: main-horizontal
    commands:
      - watch -n 10 git-sync -n
    panes:
      - type: horizontal
        commands:
          - hugo server --ignoreCache --forceSyncStatic --cleanDestinationDir --disableFastRender -e private -p 1315
  - name: notes
    layout: main-horizontal
    commands:
      - vi -c "LinnyOpenNotebook /home/casper/git/technative_b.v./technative-linny"
