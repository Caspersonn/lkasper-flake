session: workloads

root: ~/git/technative_mcs/technative-awsaccounts-workloads

windows:
  - name: workloads
    layout: main-horizontal
    commands:
      - ner

  - name: non-prod
    layout: main-horizontal
    panes:
      - type: horizontal

  - name: prod
    layout: main-horizontal
    panes:
      - type: horizontal
